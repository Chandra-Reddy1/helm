apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: welcome-app
  template:
    metadata:
      labels:
        app: nginx-demo
    spec:
      containers:
      - name: nginx-image
        image: chandra219/welcome-login-app
        ports:
        - containerPort: 80
        
